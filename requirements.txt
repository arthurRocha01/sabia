# Dependências da função (o motor em produção).
#
# As versões já validadas na prova de conceito ficam fixadas; as novas entram
# sem fixar e são travadas depois da primeira instalação bem-sucedida.
#
# Ferramenta de desenvolvimento (servidor local, testes, linter) **não** entra
# aqui: a função tem teto de tamanho na plataforma, e o servidor de
# desenvolvimento o runtime da Vercel não usa — ela roda ASGI direto. Estão em
# `requirements-dev.txt`.

fastapi
pymupdf==1.28.2
google-genai==2.22.0       # embeddings
openai==2.54.0             # interpretação (API compatível com a OpenAI)
psycopg[binary,pool]       # Postgres, com a conexão por pooler
pyjwt[crypto]
python-multipart
python-dotenv
