# Testes

Offline por padrão: as peças de fora (banco, arquivos, modelos) entram como
parâmetro e são substituídas por dublês, de modo que os testes rodem sem rede
e sem chave.

O que exige serviço real — uma ingestão ou uma consulta de verdade — é
verificação pontual, porque consome cota.
